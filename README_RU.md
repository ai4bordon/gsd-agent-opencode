# gsd-agent-opencode

[English README](README_EN.md)

`gsd-agent-opencode` — профессиональная OpenCode-first система агентной разработки с дисциплиной `command-first`: от идеи до проверки через повторяемый pipeline `context -> plan -> execute -> verify`.

Проект поставляет:
- slash-команды `/gsd-*` для управляемого процесса разработки;
- специализированных агентов (`planner`, `executor`, `verifier`, `debugger` и др.);
- шаблоны workflow/references для устойчивого выполнения без дрейфа контекста.


## Что такое GSD (исходная система)

**Get Shit Done (GSD)** - это исходная агентная система автора TACHES https://github.com/glittercowboy/get-shit-done для context engineering и spec-driven разработки: не "просто чат с ИИ", а управляемый производственный контур от идеи до проверенного результата.

По смыслу оригинального репозитория `glittercowboy/get-shit-done`, GSD решает ключевую проблему долгих AI-сессий - **`context rot`** (когда по мере роста контекста качество решений и точность исполнения деградируют).

### Что дает GSD на практике

- **Повторяемый delivery-цикл:** один и тот же понятный маршрут `new-project -> discuss -> plan -> execute -> verify`.
- **Структурированная память проекта:** артефакты (`PROJECT`, `REQUIREMENTS`, `ROADMAP`, `STATE`, `PLAN`, `SUMMARY`) позволяют продолжать работу без потери контекста между сессиями.
- **Разделение ролей через сабагентов:** planner/executor/verifier/debugger выполняют профильные задачи вместо попытки решить все в одном окне.
- **Качество через проверки, а не через обещания:** планы проверяются перед исполнением, а результат сверяется с целями после исполнения.
- **Трассируемость изменений:** атомарные шаги и понятный ход работ упрощают возврат, аудит и сопровождение.

### Чем GSD отличается от многих других AI-workflow систем

- **Меньше процессного "театра":** в оригинальном позиционировании GSD делается акцент на практическом построении фич, а не на перегруженных церемониях.
- **Не только генерация кода:** GSD покрывает полный контур (контекст, требования, планирование, выполнение, верификация), а не единичный "сгенерируй файл" сценарий.
- **Операционная дисциплина вместо ad-hoc чата:** система ведет по шагам и хранит состояние, что снижает случайные развороты в процессе.
- **Сильный упор на воспроизводимость:** один и тот же pipeline можно повторять по фазам и milestone, получая сопоставимое качество.

### Ключевые преимущества перед ad-hoc подходом

- **Меньше регрессий и "почти готово":** благодаря обязательным этапам проверки и gap-циклам.
- **Быстрее возврат в проект после пауз:** состояние уже зафиксировано в `.planning/`.
- **Лучше масштабируется на длинные задачи:** за счет декомпозиции и разнесения работы по сабагентам.
- **Выше управляемость качества:** есть явные артефакты, критерии done и последовательность действий.

Оригинальный репозиторий GSD: https://github.com/glittercowboy/get-shit-done

## Отличия этой версии

- OpenCode-first реализация GSD с упором на стабильный командный pipeline в OpenCode.
- Добавлен primary-оркестратор `Gsd-agent`, который диспетчеризирует весь цикл и навязывает дисциплину `Next command`.
- Введена строгая continuation-политика: ответы вроде `ok/continue/давай` запускают именно pending-команду.
- Усилен анти-дрейф контроль через приоритет команд: `explicit > pending > autonomy`.
- Интегрирован RALF Lite для ограниченных retry-петель с понятными stop-условиями.


<table>
  <thead>
    <tr>
      <th>Критерий</th>
      <th>Upstream Get Shit Done</th>
      <th>Эта версия (<code>gsd-agent-opencode</code>)</th>
      <th>Что это дает на практике</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>🎯 Фокус дистрибуции</td>
      <td>Универсальная мульти-рантайм система (Claude/OpenCode/Gemini)</td>
      <td>OpenCode-first дистрибуция и UX под OpenCode</td>
      <td>Меньше адаптационных компромиссов именно в OpenCode</td>
    </tr>
    <tr>
      <td>🧭 Главный оркестратор</td>
      <td>Общая оркестрация через команды и workflow</td>
      <td>Явный primary-агент <code>Gsd-agent</code> как диспетчер всего цикла</td>
      <td>Более предсказуемое движение по pipeline</td>
    </tr>
    <tr>
      <td>▶️ Политика продолжения</td>
      <td>Стандартный переход между командами</td>
      <td>Жесткая <code>pending command</code> дисциплина: <code>ok/continue/давай</code> исполняет последний <code>Next command</code> буквально</td>
      <td>Меньше "срывов" в свободный чат</td>
    </tr>
    <tr>
      <td>🔀 Маршрутизация изменений</td>
      <td>Общие правила workflow</td>
      <td>Primary-agent принудительно маршрутизирует repo-changing действия через <code>/gsd-*</code> и сабагентов</td>
      <td>Выше трассируемость и управляемость изменений</td>
    </tr>
    <tr>
      <td>🧱 Анти-дрейф контроль</td>
      <td>Базовая контекстная дисциплина GSD</td>
      <td>Доп. command priority policy: <code>explicit &gt; pending &gt; autonomy</code></td>
      <td>Ниже риск, что ассистент подменит явную команду пользователя</td>
    </tr>
    <tr>
      <td>🔁 Петли автопочинки</td>
      <td>Базовые циклы verify/fix</td>
      <td>Явно интегрирован RALF Lite (bounded retry loops с условиями остановки)</td>
      <td>Нет бесконечных автоповторов без прогресса</td>
    </tr>
  </tbody>
</table>

### Что решает добавление `Gsd-agent` (primary orchestrator)

`agents/Gsd-agent.md` добавляет слой диспетчеризации, который закрывает типовые проблемы длинных AI-сессий:

- **Проблема:** после нескольких шагов ассистент уходит в обсуждение вместо выполнения следующей команды.  
  **Решение:** политика pending-команды + literal continuation (`ok/continue/давай` запускают именно последний `Next command`).
- **Проблема:** ассистент заменяет явную команду пользователя на "более логичную" на свой взгляд.  
  **Решение:** строгий приоритет правил: explicit command > pending command > общая автономия.
- **Проблема:** primary-агент начинает сам напрямую менять репозиторий и теряется трассируемость этапов.  
  **Решение:** primary работает как роутер/диспетчер; repo-changing действия должны идти через `/gsd-*` pipeline и профильных сабагентов.
- **Проблема:** gaps после verify/UAT могут быть пропущены.  
  **Решение:** обязательный gap-loop маршрут (`/gsd-plan-phase --gaps` -> `/gsd-execute-phase --gaps-only`).

Итог по сравнению с исходной системой: эта версия делает поведение в OpenCode более детерминированным и операционно устойчивым именно в сценарии "ведем проект итерациями до готового результата".

## Что внутри

<table>
  <thead>
    <tr>
      <th>Слой</th>
      <th>За что отвечает</th>
      <th>Где лежит</th>
      <th>Что получает пользователь</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code>/gsd-*</code> команды</td>
      <td>Точки входа, маршрутизация этапов, запуск сабагентов</td>
      <td><code>commands/gsd/</code></td>
      <td>Понятные действия "что запускать дальше"</td>
    </tr>
    <tr>
      <td>Агенты</td>
      <td>Специализированные роли pipeline</td>
      <td><code>agents/</code></td>
      <td>Более точное выполнение задач по ролям</td>
    </tr>
    <tr>
      <td>Workflow-правила</td>
      <td>Протоколы выполнения, проверки, резюма, дебага</td>
      <td><code>get-shit-done/workflows/</code></td>
      <td>Стабильный и повторяемый процесс</td>
    </tr>
    <tr>
      <td>References</td>
      <td>Политики checkpoint, RALF Lite, профили моделей, config</td>
      <td><code>get-shit-done/references/</code></td>
      <td>Контроль качества и предсказуемость поведения</td>
    </tr>
    <tr>
      <td>Состояние проекта</td>
      <td>Источник истины между сессиями</td>
      <td><code>.planning/</code></td>
      <td>Быстрый возврат в работу после пауз</td>
    </tr>
  </tbody>
</table>

## Основной pipeline

Для фазы `N`:

1. `/gsd-discuss-phase N` — уточнение решений и снятие неоднозначности.
2. `/gsd-plan-phase N` — исследование (опционально), генерация плана, проверка плана.
3. `/gsd-execute-phase N` — выполнение плана по задачам/волнам.
4. `/gsd-verify-work N` — UAT и закрытие пробелов через gap-loop.

Если непонятно, что делать дальше: `/gsd-progress`.

## Ключевые команды

### Запуск и основной поток

- `/gsd-new-project` — инициализация: контекст, требования, roadmap.
- `/gsd-map-codebase` — анализ существующего проекта перед новым milestone/roadmap.
- `/gsd-discuss-phase N` — фиксация продуктовых/технических решений по фазе.
- `/gsd-plan-phase N` — создание исполнимого `*-PLAN.md` с проверкой качества плана.
- `/gsd-execute-phase N` — выполнение задач с checkpoint-протоколом.
- `/gsd-verify-work N` — UAT, верификация и запуск gap-плана при необходимости.

### Управление процессом

- `/gsd-progress` — текущий статус и следующая рекомендуемая команда.
- `/gsd-pause-work`, `/gsd-resume-work` — корректная пауза/продолжение.
- `/gsd-audit-milestone`, `/gsd-complete-milestone <version>` — аудит и закрытие milestone.
- `/gsd-add-phase`, `/gsd-insert-phase`, `/gsd-remove-phase` — изменение roadmap по фазам.

### Операционные команды

- `/gsd-debug` — структурированный разбор дефектов.
- `/gsd-quick` — быстрый режим для небольших задач.
- `/gsd-research-phase` — targeted research до/во время планирования.
- `/gsd-settings`, `/gsd-set-profile`, `/gsd-set-model` — управление профилями и моделями.
- `/gsd-help`, `/gsd-whats-new`, `/gsd-update` — справка и обновления.

## Артефакты в `.planning/`

Базовые файлы состояния:
- `.planning/PROJECT.md`
- `.planning/REQUIREMENTS.md`
- `.planning/ROADMAP.md`
- `.planning/STATE.md`

Артефакты по фазам:
- `*-PLAN.md`
- `*-SUMMARY.md`
- `*-VERIFICATION.md`
- `*-UAT.md`

## Требования

- Node.js `>= 18`
- OpenCode, который загружает slash-команды из `commands/`

## Установка

Поддерживаются 2 scope:
- `--global` -> `${OPENCODE_CONFIG_DIR:-~/.config/opencode}`
- `--local` -> `./.opencode` текущего проекта

### Вариант A (рекомендуется): установка CLI из GitHub

```bash
npm i -g github:ai4bordon/gsd-agent-opencode
gsd-agent-opencode install --global
```

Локальная установка в проект:

```bash
gsd-agent-opencode install --local
```

### Вариант B: запуск установщика напрямую из репозитория

```bash
node bin/gsd.js install --global
node bin/gsd.js install --local
```

### Вариант C: one-shot через `npx`

```bash
npx github:ai4bordon/gsd-agent-opencode --global
npx github:ai4bordon/gsd-agent-opencode --local
```

## Проверка, обновление, удаление

```bash
gsd-agent-opencode list
gsd-agent-opencode check
gsd-agent-opencode update --global
gsd-agent-opencode uninstall --global
```

Полезно:
- dry-run удаления: `gsd-agent-opencode uninstall --global --dry-run`
- кастомный глобальный путь: `gsd-agent-opencode install --global --config-dir <path>`

## Быстрый старт в проекте

1. Перезапустите OpenCode после установки.
2. Для существующего проекта: `/gsd-map-codebase`, затем `/gsd-new-project`.
3. Для нового проекта: сразу `/gsd-new-project`.
4. Работайте по фазам через `/gsd-plan-phase` -> `/gsd-execute-phase` -> `/gsd-verify-work`.

## Документация

- Полный гайд (RU): `docs/GSD_AgentSystemGuide.html`
- Full guide (EN): `docs/GSD_AgentSystemGuide_EN.html`
- Primary orchestrator: `agents/Gsd-agent.md`
- RALF Lite: `get-shit-done/references/ralf-lite.md`
- Checkpoints: `get-shit-done/references/checkpoints.md`
- Planning config: `get-shit-done/references/planning-config.md`

## Благодарности

- Исходная система **Get Shit Done**: https://github.com/glittercowboy/get-shit-done
- OpenCode-адаптация, на которую опирался проект: https://github.com/rokicool/gsd-opencode
- Спасибо всем контрибьюторам за развитие command-first практик агентной разработки.

## Лицензия

MIT, см. `LICENSE`.
